import { TransformExercicioPipePipe } from './transform-exercicio-pipe.pipe';

describe('TransformExercicioPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformExercicioPipePipe();
    expect(pipe).toBeTruthy();
  });
});
